apiVersion: apps/v1 
kind: Deployment
metadata:
  name: nile
  labels: 
   app: claudera
spec:
  replicas: 2
  selector:
    matchLabels:
      app: claudera
  template:
    metadata:
     labels:
       app: claudera
    spec:
      containers:
      - name: cont1
        image: cronosm4m/jenkargo:latest



---
apiVersion: v1 
kind: Service
metadata:
  name: jenkargo
spec:
  type: NodePort
  selector:
    app: claudera
  ports:
  - port: 80  #this is for the service
    targetPort: 80 #this is for the container port.
